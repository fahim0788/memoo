# =============================================================================
# MemoList MVP - Variables d'environnement
# =============================================================================
# Ce fichier est utilisé par Docker Compose (mode production/docker)
#
# Pour le développement local :
# - Ce fichier .env → utilisé par docker-compose.yml
# - apps/api/.env   → utilisé par l'API en mode dev local (DATABASE_URL=localhost)
#
# Copier ce fichier vers .env et adapter les valeurs
# cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Frontend (Web)
# -----------------------------------------------------------------------------
# Base URL de l'API (relative pour Docker/Nginx, absolue pour dev séparé)
NEXT_PUBLIC_API_BASE=/api

# Nom de l'application affiché
NEXT_PUBLIC_APP_NAME=MemoList

# -----------------------------------------------------------------------------
# Backend (API)
# -----------------------------------------------------------------------------
# Port de l'API (utilisé en dev local uniquement)
API_PORT=3001

# URL de connexion PostgreSQL
# - Dev local:  postgresql://memolist:memolist@localhost:5432/memolist
# - Docker:     postgresql://memolist:memolist@db:5432/memolist
DATABASE_URL=postgresql://memolist:memolist@db:5432/memolist

# Secret pour signer les tokens JWT (CHANGER EN PRODUCTION!)
# Générer avec: openssl rand -hex 32
JWT_SECRET=dev-jwt-secret-change-in-production

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------
POSTGRES_DB=memolist
POSTGRES_USER=memolist
# CHANGER EN PRODUCTION!
POSTGRES_PASSWORD=memolist

# -----------------------------------------------------------------------------
# Storage TTS (audio files)
# -----------------------------------------------------------------------------
# Type de stockage : local (dev) | minio (prod)
STORAGE_TYPE=local

# Local storage (dev)
LOCAL_STORAGE_PATH=./storage/tts
TTS_STORAGE_PATH=../worker/storage/tts

# URL publique des fichiers audio
PUBLIC_BASE_URL=http://localhost:3001/storage/tts
