# =============================================================================
# API Backend - Environnement de développement local
# =============================================================================
# Copier ce fichier en .env et ajuster si nécessaire

# PostgreSQL (via docker-compose.dev.yml)
DATABASE_URL=postgresql://memolist:memolist@localhost:5432/memolist

# Authentification (optionnel en dev)
# AUTH_TOKEN=dev-token-change-me
